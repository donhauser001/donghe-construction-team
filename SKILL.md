---
name: donghe-construction-team
description: "Engineering discipline (东合施工队) for implementing and fixing code: evidence-backed completion, scoped writes, shared-workspace coordination and parallel support work. Use for coding, 开工/施工 or authorized unattended work. Pure questions and read-only decision lookup do not start construction workflows."
---

# 东合施工队 · v0.9.0

核心：真实证据、明确写域、诚实汇报；以减少施工和接手成本为目标。

## 默认工作方式

- 在用户指定的同一个物理工作区施工。**不得自行创建 worktree、克隆或复制项目作为施工环境；只有用户明确指定隔离时才执行。** 未提交修改、多人并行、跨层任务或冲突都不是自动隔离理由。既有副本不自动删除。
- 先看项目 AGENTS 与当前施工入口，认领前检查目标和写域是否重叠；只读本任务相关资料。既有修改按归属保留，共享文件交一个责任人，重叠工作串行。
- 沿用项目已指定的施工系统。**目录存在不代表采用 CLI**，包括试点留下的 docs/东合/；不得自动 init、迁移或生成第二套任务、日志、交接。无项目系统时使用轻量路径；用户或项目明确采用 CLI 后才走 playbook09。
- doctor 仅在安装、升级、运行异常时按需执行，不是每次开工仪式。已有授权与偏好持续有效，不反复询问。

## 实施与证据

1. 开工前简述目标和写域；顺路发现的无关问题记录后继续原任务，不擅自扩线。
2. 验证与风险相称。类型检查和构建不能替代功能行为；UI 变化验证实际入口、操作和反馈。说明已验证、未验证及原因，不用完成声明掩盖盲区。
3. 优先复用项目测试与证据系统；需要收集器时使用技能包 scripts/collect_evidence.py（包内 runtime/python/bin/python3 -I -B）或等价真实命令输出。非零退出不算通过。证据绑定当次输入；并行期间受测输入改变，该回执不能证明变更后的代码通过。
4. 验证通过后，只有新变更、失败或未解疑点才扩大/重复验证。代码事实与设计冲突时区分实然与应然，不能用旧实现撤销已批准设计。

| 规模 | 执行方式 |
|---|---|
| S：小范围低风险 | 直接完成，做必要检查，简短汇报 |
| M：单模块 | 明确验收，主 agent 实现，独立支线按需委派 |
| L：跨层、schema、权限安全等 | 先明确共享契约、依赖和验收，再按独立性安排并行或串行 |

任务卡遵循项目系统；没有系统时，仅需要持久执行合同的复杂实施使用 templates/任务卡模板.md。短只读调查使用派工消息即可，不为每个子 agent 新建文档。

## 文件规模与职责

- 手写代码与样式文件以 **不超过 500 行**为目标，按项目正常格式化后的物理行数计量。小幅超出允许；默认将 **600 行作为拆分触发线**，项目有更严格标准时遵循项目标准。
- 改动前检查目标文件规模和职责；接近 500 行时先设计模块边界，超过 500 行时检查是否混入第二职责。新建或实质扩展的文件预计超过 600 行，先按职责拆分再继续，不把新能力不断塞进现有入口文件；确无合理拆分边界时说明具体原因，不能为了行数制造牵强模块，也不能据此持续扩展至数千行。交付时只需简述本次新建或实质扩展的超标手写文件与处理理由，存量文件局部小修不另加规模报告。
- 已有数千行文件不要求每次小修全量重构：局部修错可保持最小改动；新增独立能力放入独立模块，本次必须触及的职责在安全且相关的范围内提取。不得以历史文件已超标为由继续大块追加；提取会扩大风险时先完成必要小修，如实记录剩余问题，不承诺未经授权的重构。
- CSS/SCSS 等同样遵守：按组件或功能归属组织，公共层只保留共享 token、基础样式与真正复用的规则；避免在全局文件末尾层层覆盖。拆分检查导入顺序、选择器作用域、级联优先级及媒体查询，验证实际界面，防止文件变小却样式失效。
- 按职责、依赖和可读性拆分，不按行号机械分成 part1/part2，不制造循环依赖，也不以压缩多行、删除必要注释或把代码藏进字符串绕过规模目标。
- 生成代码、第三方源码、锁文件等非手写产物不套用此限额，也不手改；手写测试仍按场景组织。项目契约要求保持单文件时说明具体依据和规模，不能由 agent 自行宣布通用豁免。

## 子 agent 调度

- **主动委派**可独立的文档对齐、事实调查、资料查阅、影响分析与写域独立的实现。顺手能做完的小事直接做，不为派工而拆分。
- 派工消息写清目标、来源、允许写域（只读则明确）、交付物和依赖；主 agent 保留架构取舍、共享契约和最终核验责任。
- 派出后立即继续不依赖支线结果的施工。只在下一步确实依赖结果时等待该项；不要因一条支线未完成让整个主进程停工，也不能猜测依赖结论继续实现。
- 所有子 agent 使用同一指定工作区，最多 3 个并行工人且不超过宿主限制；同一文件一个写入责任人。Git 提交、切分支、索引和归档等共享操作由主 agent 协调。
- 模型按宿主支持与任务难度选择，见 references/models.md；派前简述人数和职责。无子 agent 能力则串行，不安装额外系统。
- 回报后核验来源、diff、写域和证据；调查建议不得自行成为用户裁定。必要支线在整项交付前收口，非阻塞发现如实记录。

## 文档对齐与决策查询

- **一项事实只维护一个正本。** 施工状态进入项目施工系统；设计裁定进入相应架构正本；API/配置用法进入对应使用文档。AGENTS、README 和索引只链接入口，避免重复写焦点、任务数量和测试数字。
- 当前交接替换为当前快照，历史过程追加到现有记录；不靠在旧结论前不断追加更正维护当前状态，不另建对齐台账。
- 用户裁定在依照它施工前落到正本并标明替代范围；认领、写域变化和阻塞在其他队依赖前登记；完成/验收在报告前记录。无需按分钟写心跳或每改一行写日志。
- 文档对齐可派独立支线，提供已确认的裁定和改动；涉及主施工依赖的关键约束先同步，其他文档并行处理，交付前核验。接手和恢复时重读相关最新源。
- 自动摘要只能由唯一源派生；无法刷新则标记陈旧并回读来源。检查链接及章节锚点，不能把结构闸通过当作语义一致。
- 遇到决策问题，可使用已提供的独立只读决策查询技能，或直接定位正本相关章节。返回依据、适用范围、例外和未裁定冲突；不为查询创建任务、复制知识正文、启动 CLI 或读取全量施工档案。没有查询技能也可继续。

## 项目阅读入口

- 用户在明确项目授权施工时，自动检查根目录 `东合项目进化史.html`；入口缺失或项目尚未注册时用 `bin/donghe --project <根> reader attach` 建立只读阅读入口，不让用户填路径、端口或执行命令。已接入且入口有效时不重复全量刷新。纯问题与只读查询不触发接入。
- 阅读接入独立于 CLI 治理采用：不执行 init、不迁移旧资料、不创建第二套任务卡。沿用原有文档与明确任务线关系，无法确定归属的任务标待归类。
- 用户从根 HTML 看快照，点击“连接实时资料”唤起包内助手，再用页面刷新/重建按钮读取已落盘资料。更新不依赖施工正常结束；中断和待裁决状态保持原文，未写入文件的会话状态不得猜测。读取策略与平台边界见 `playbooks/13-项目阅读入口.md`。

## 按需资源

- 明确采用 CLI 的任务：playbooks/09-最小完工闭环.md。
- 明确使用 CLI 管理资料或月度归档：playbooks/10-资料关联与月度归档.md；单写者操作。
- 安装、升级、代码图谱定位：playbooks/11-完整包与代码定位.md。
- UI 改动：playbooks/12-真实界面验收.md。
- 用户要求持续施工：references/unattended.md；不扩大授权范围。

## 发行

版本 v0.9.0。正本为 donhauser001/donghe-construction-team 仓库；修改源码，再由 scripts/build_release.py 构建完整包、bin/install 同步 Codex/Cursor 并备份。不要直接编辑安装目录。完整包安装同时注册本机阅读助手，无登录常驻服务；平台范围仍为 macOS arm64。版本史见 CHANGELOG.md。
