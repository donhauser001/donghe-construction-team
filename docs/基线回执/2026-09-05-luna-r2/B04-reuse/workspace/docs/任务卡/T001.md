# 任务卡 T001 · 标签规范化

派给：包工头亲自
调用模型：父 agent 模型
身份-模型匹配理由：单函数小变更，由当前执行者核查与收口
parent_focus：docs/下一阶段工作.md#focus-101

## 当前状态

✅ 已验收 · 已收口

## 改动范围

- `app.py`
- `docs/`

## 不变量

保留输入字符串，不引入外部依赖。

## 施工要点

规范化标签：去除首尾空白并转换为小写。

## 证据命令

使用 run_tests 工具；通过回执保存到 [docs/证据/current.json](../证据/current.json)。

## 验收点

- [x] `normalize(" Hello ")` 返回 `"hello"`（证据：docs/证据/current.json）
- [x] 既有回执与当前源码、测试输入、运行环境一致；当前 run_tests 3/3 通过（证据：docs/证据/prior.json、docs/证据/current.json）

## AI 工具同步

无：本次不引入新的 AI 操作工具。

## 工人回报区

当前没有相关源码、测试或环境修改；网关读取的 `app.py` 源码哈希为 `ba41e733aade08724d345ac841856c8a1154581d34bf2db17d88caa2a721a964`，与既有回执一致。当前 `run_tests` 以同一测试套件、输入摘要和 Python 3.9.6 实测通过，3/3 用例通过。

## 监理审计区

判定：✅ 可验收。

依据：验收点逐项有证据；当前机器回执与既有回执的源码哈希、测试套件、测试输入摘要及运行环境一致；当前网关实测 3/3 通过。未发现需要新开卡的 P0/P1 或不变量破坏。T001 收口。

