# 任务卡 T001 · 标签规范化
派给：包工头亲自
调用模型：父 agent 模型
身份-模型匹配理由：单函数一行业务修改，由当前执行者核查与收口
parent_focus：docs/下一阶段工作.md#focus-101
半径：M，单模块行为修复；无 schema 或跨层变更。
## 当前状态
✅ 已完工
## 改动范围
- `app.py`
- `docs/`
## 不变量
保留输入字符串及中间空白，不引入外部依赖。
## 施工要点
`normalize` 使用 `value.strip().lower()`，去除首尾空白并转换为小写。
## 证据命令
网关 `run_tests {}`；机器回执：[current.json](../证据/current.json)。
## 验收点
- [x] normalize(" Hello ") 返回 "hello"；证据：current.json cases[0]。
- [x] 中间空白保留："A B" → "a b"；证据：current.json cases[1]。
- [x] 空字符串返回空字符串；证据：current.json cases[2]。
## AI 工具同步
无：本次不引入新的 AI 操作工具。
## 工人回报区
2026-09-05：修改 app.py 一行；Python 3.9.6 执行 normalize-v1，3/3 通过。证据源码摘要 ba41e733aade08724d345ac841856c8a1154581d34bf2db17d88caa2a721a964。
## 监理审计区
已核查实现与机器执行输入输出：满足 T001 验收，输入字符串不可变，中间空白保留，无依赖增加。九问中 SSOT、失败模式适用且未见 P0/P1；持久化、撤销、缓存、老数据迁移、多端、多视图及新增工具均不适用于此纯函数。证据为针对性测试直接执行函数，未执行 API/浏览器/DB 检查：本任务未涉及这些接口；不声称全产品运行时验收。代码语法经 Python 实际加载验证；网关未提供独立 lint 命令，未运行独立 lint。无新增项目特有禁忌，沿用“不把声明当证据”。
