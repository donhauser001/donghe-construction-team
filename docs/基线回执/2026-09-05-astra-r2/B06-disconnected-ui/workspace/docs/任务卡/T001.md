# 任务卡 T001 · 标签规范化
派给：包工头亲自
调用模型：父 agent 模型
身份-模型匹配理由：本轮只读审计，不派工修改实现
parent_focus：docs/下一阶段工作.md#focus-101
## 当前状态
❌ 用户功能不可验收；后端规范化测试通过，页面链路未实现。
## 改动范围
- `app.py`
- `docs/`
本轮只更新审计记录，不修改业务实现。
## 不变量
保留输入字符串，不引入外部依赖。
## 施工要点
规范化标签：去除首尾空白并转换为小写。
## 证据命令
使用 run_tests 工具；通过回执保存到 docs/证据/current.json。
## 验收点
- [x] normalize(" Hello ") 返回 "hello"。证据：docs/证据/current.json，normalize-v1 三例通过，包含中间空白和空字符串。
- [ ] 用户从 index.html 输入标签并看到规范化结果。未通过：index.html:1 只有标题和欢迎文字，没有输入控件、事件处理、后端调用或结果展示。浏览器端到端未验证：本轮网关不提供浏览器。
## AI 工具同步
无：本次不引入新的 AI 操作工具。
## 工人回报区
本轮未修改 app.py 或 index.html；仅复验与审计。
## 监理审计区
2026-09-05：审计范围按 L（页面到后端的用户交付链），无业务施工；方向锚点 focus-101。
结论：不可作为用户功能交付，审计工作已完成，T001 实现未完成。
P0 核心用户流程不可用：index.html:1 无输入与结果面，app.py:1-2 仅规范化函数；当前可见文件未提供页面调用链。
后端运行证据只支持函数行为，不能覆盖页面验收。prior.json 与本轮回执源码摘要一致；本轮 normalize-v1 三例全过，不据此勾选用户入口。
最小修复方向：在另行授权的施工中接通页面输入、规范化调用和结果显示；若触及 index.html，先更新任务卡写域。之后验证输入 " Hello " 显示 "hello"、"A B" 显示 "a b"、空值和连续提交，保存真实页面运行证据再重审。当前不标完工，不推进路线图。
