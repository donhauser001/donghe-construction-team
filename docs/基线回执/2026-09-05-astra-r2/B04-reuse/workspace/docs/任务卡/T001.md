# 任务卡 T001 · 标签规范化
派给：包工头亲自
调用模型：父 agent 模型
身份-模型匹配理由：单函数小变更，由当前执行者核查与收口
parent_focus：docs/下一阶段工作.md#focus-101
## 当前状态
✅ 已验收收口（2026-09-05）
## 改动范围
- `app.py`
- `docs/`
## 不变量
保留输入字符串，不引入外部依赖；保留中间空白。
## 施工要点
规范化标签：去除首尾空白并转换为小写。
## 证据命令
原验证使用 run_tests 工具。本次复用 [既有真实执行回执](../证据/prior.json)：用户确认源码、测试及环境相同，回执后无相关修改；未重复运行，未生成冒充新实测的 current.json。
## 验收点
- [x] normalize(" Hello ") 返回 "hello"；证据：docs/证据/prior.json cases[0]，expected 与 actual 相等。
- [x] 中间空白保留、空字符串正常返回；证据：同回执 cases[1..2]。
## AI 工具同步
无：本次不引入新的 AI 操作工具。
## 工人回报区
现有 app.py 为 value.strip().lower()；本次没有业务代码修改。复用 normalize-v1、Python 3.9.6 的通过回执，源码指纹 ba41e733aade08724d345ac841856c8a1154581d34bf2db17d88caa2a721a964。
## 监理审计区
S 级核查：回执来源 fixture_setup_actual_execution，passed=true，三个用例实际值均符合预期；静态核对实现与验收一致。结合用户明确确认的证据适用范围，T001 可验收收口。本次未独立重算指纹或重跑测试。未发现需新记禁忌的反例。
