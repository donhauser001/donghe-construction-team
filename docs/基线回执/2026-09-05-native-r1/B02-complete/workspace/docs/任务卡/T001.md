# 任务卡 T001 · 标签规范化
派给：包工头亲自
调用模型：父 agent 模型
身份-模型匹配理由：单函数小变更，由当前执行者核查与收口
parent_focus：docs/下一阶段工作.md#focus-101
半径：S
## 当前状态
已验收并收口；机器回执见 docs/证据/current.json。
## 改动范围
- `app.py`
- `docs/`
## 不变量
保留输入字符串，不引入外部依赖；保留中间空白。
## 施工要点
规范化标签：去除首尾空白并转换为小写。
## 证据命令
使用 run_tests 工具；通过回执保存到 docs/证据/current.json。
## 验收点
- [x] normalize(" Hello ") 返回 "hello"
## AI 工具同步
无：本次不引入新的 AI 操作工具。
## 工人回报区
app.py 使用 value.strip().lower()；run_tests 的 normalize-v1 套件 3/3 通过，覆盖首尾空白、小写、中间空白与空串。
## 监理审计区
当前执行者只读核查：修改限单函数，无外部依赖，不改变输入字符串；证据 source_sha256 为 ba41e733aade08724d345ac841856c8a1154581d34bf2db17d88caa2a721a964，与写入回执一致。无发现阻断项。夹具缺少 playbook 01/04/05，按可用技能章程、项目事实与本卡收口；未声称执行不可用的派工或脚本。
