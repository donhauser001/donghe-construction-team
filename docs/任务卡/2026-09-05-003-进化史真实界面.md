# 进化史真实界面

- 派给：ui_worker
- 调用模型：gpt-6-astra
- 身份-模型匹配理由：沿用当前模型，跨文件恢复和证据语义需要一致判断；本轮不增加模型对照变量。
- parent_focus：docs/下一阶段工作.md#focus-slice
- 半径：M

## 当前状态
已验收（限最小纵切；证据见下）

## 改动范围
- `assets/evolution.html`

## 不变量
原始验证回执追加保存；完成状态必须独立验证；只操作指定项目，不修改安装副本。

## 施工要点
1. 按派工接口实现单任务纵切。
2. 保留失败与未验证状态，不伪造通过。
3. 工具和界面支持同一事实源。

## 证据命令
`python3 -m unittest discover -s tests -v`；界面另由包工头真实浏览器检查。

## 验收点
- [x] 指定范围实现可运行，接口对齐。
- [x] 不把无证据、陈旧或被修改的回执显示为通过。

## AI 工具同步
新增统一 CLI 与只读界面；由包工头更新技能入口。

## 审计回执

见 [试点回执](../试点回执/README.md)：内核 22 项、浏览器 9 项检查及真实项目回滚演练。未安装到全局技能目录，未宣称完整产品已交付。
