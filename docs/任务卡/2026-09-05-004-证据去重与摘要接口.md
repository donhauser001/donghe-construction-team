# 证据去重与摘要接口

- 派给：evidence_core
- 调用模型：gpt-6-astra
- 身份-模型匹配理由：沿用当前模型，聚焦证据完整性与真实界面，保持质量对照变量稳定。
- parent_focus：docs/下一阶段工作.md#focus-evidence
- 半径：M

## 当前状态
已验收（2026-09-05）

## 改动范围
- `scripts/donghe.py`
- `tests/test_donghe.py`

## 不变量
旧回执不改；共享指纹损坏必须使引用者失效；历史失败与当前验收分域；原始输出仍可访问。

## 施工要点
1. 按派工接口实现边界内功能。
2. 保留来源与摘要校验，不以隐藏失败压缩数据。
3. 完成相关自动验证和接口对齐。

## 证据命令
`python3 -m unittest discover -s tests -v`；`node evals/pilot/evolution-ui.cjs`（参数由本机环境注入）。

## 验收点
- [x] 新旧格式可读且篡改/缺失不误判通过。
- [x] 验证或界面回执可复现。

## AI 工具同步
保留原CLI；新增只读/api/receipt摘要，原文按需读取。

验收回执：[证据改进实测](../证据改进回执/README.md)。
