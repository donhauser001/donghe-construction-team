# 完整安装包独立验收

- 对象：`output/release/donghe-construction-team-v0.7.0-rc.1-darwin-arm64.tar.gz` 及当前候选构建/安装实现。
- 平台口径：仅 macOS arm64；实测环境 Darwin arm64。
- 只读范围：`scripts/build_release.py`、`scripts/install_release.py`、`bin/`、`packaging/components.lock.json`。
- 写域：本卡与 `docs/完整发行回执/install-review.md`；行为复现仅用临时目录。
- 验收点：路径与符号链接安全、组件和文件 checksum、安装失败回滚、`PATH=/usr/bin:/bin` 首用、无 Python 外部依赖。
- 初审裁定：不可验收。多目标失败不回滚已安装目标；manifest 不守恒可执行权限且不 smoke 图谱，均为 P1。
- 修复后裁定：macOS arm64 首版可验收。全目标两阶段安装与逆序回滚、mode 校验、双 healthcheck 已落地；正式 `v0.7.0` 重建包通过真包成功路径、mode 篡改拒绝和第二目标故障回滚。
- 已通过：外层 SHA-256、普通文件篡改拒绝、目标 symlink 拒绝、旧版备份、最小 PATH 安装与首次 init、Python 3.12.14 和 codegraph 0.8.1 原样包运行。
- 边界：只修改安装器和相关测试，未跑全 suite，未提交；不声明其他平台可用。
