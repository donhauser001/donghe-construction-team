# 任务卡 · 建立旧版行为基线

派给：包工头亲自
调用模型：父 agent 模型
身份-模型匹配理由：评测判据由主 Agent 维护，受测样本由隔离模型上下文执行
半径：M
parent_focus：docs/下一阶段工作.md#focus-baseline

## 当前状态
✅ 已验收（受限环境首轮基线）

## 改动范围
- `evals/behavior/`
- `docs/`
- `.gitignore`
- `README.md`

## 不变量
旧版 SKILL、playbooks、scripts、templates 保持原样；不修改业务仓库代码或安装目录；不记录凭证。

## 施工要点
1. 从固定提交生成完整旧技能快照及六个初态。
2. 实跑旧脚本与两档模型，保存所有调用、终态和判定边界。
3. 独立复核事实，将环境/夹具失败与模型行为分开。

## 证据命令
`python3 -m unittest discover -s evals/behavior -v`
`python3 scripts/check_card.py docs/任务卡/2026-09-05-001-建立旧版行为基线.md`
`bash scripts/lint.sh`

## 验收点
- [x] 六场景可执行夹具及判据可复跑；评测工具 7 项自测通过。
- [x] 旧脚本有真实 stdout/退出码回执，见 scripts-r2。
- [x] Astra/Luna 各六个独立样本，原始记录与复核分开。
- [x] [结果报告](../基线回执/README.md)明确样本量、宿主限制、未测能力与改造建议。

## AI 工具同步
无：评测专用网关，不修改产品工具面。

## 工人回报区
已实现隔离网关、固定提交快照、脚本探针、两种运行入口、汇总及六个业务初态；旧技能未修改。未做：完整原生 CLI/浏览器、稳定成功率估计、真实事务恢复、新技能改造。

## 监理审计区
主 Agent 已核对 12 个有效样本的回执、调用和终态；旧 native-r1 夹具失误整轮作废，保留原始记录。修正两项机械误判；源规则 lint 与卡校验通过。实测材料见 [基线报告](../基线回执/README.md)。
