# v0.9.0 项目阅读入口验证

## 范围

项目根目录 `东合项目进化史.html` 离线快照；任务线与任务流水；原文、锚点、基础表格、全文检索和历史资料；页面刷新与重建索引；完整包内置 Python 与本机按需 URL 助手。保留现有 MD 治理和所有业务源，不执行 init、归档、测试、续工或完工登记。

## 源码验证

- Python 测试 123 项全部通过、无跳过：设置 `DONGHE_UI_TEST_BINARY` 指向已安装完整包的 Chrome Headless Shell 后运行 unittest discover。包含原 UI 浏览器用例及新增目录扫描、归类、安全、刷新和空闲退出用例。
- skill-creator quick_validate 通过；git diff --check 通过。
- 新阅读页独立 Chrome 验证：离线快照不越权请求 localhost、任务原文、搜索与历史入口、HTTP 刷新与重建、失败保留快照、390px 布局、文档锚点与表格；测试脚本 `tests/workspace_reader_browser.mjs`。本机证据 `output/reader-browser/result.json` 和截图（派生产物不入源码提交）。
- 完整包原生助手冒烟：打包 Python 驱动 osacompile 注册 `donghe://`，经 macOS `open` 实际唤起后，已登记项目读取进程健康检查成功。助手无登录启动项，单项目进程空闲 15 分钟退出；浏览器首次外部应用确认不可免除。

## 诚实边界

资料仅覆盖根 README/AGENTS 和 docs 中有界 Markdown。归属只取显式 parent_focus/task_line，缺失则待归类；编号不是依赖或真实执行时序，任务记录不证明 Agent 此刻运行。仅源码已经落盘的状态可刷新；历史不等于已通过。两个更新操作均完整重扫，不宣称增量索引。HTML 包含内部文档，不自动提交或发布。

## 正式包与项目现场

首轮正式包源提交 `b8e7d6d` 已推送 origin/main，并经完整包安装器更新 Codex/Cursor；双端 reader 资产、索引模块与 SKILL 字节核对一致。备份位于 `~/.donghe/backups/20260907-234105-d5c9565e/`。doctor 返回 ready，无首次依赖下载。

macOS `donghe://` 从正式安装路径实际唤起 `/Users/aiden/dev/dhs-paiban-main` 阅读器。根入口生成成功，扫描 1306 份文档（其中 974 份历史），6 组明确归属与 11 个待归类任务。现场 Chrome 七项行为验证通过、零页面错误；本机证据 `output/reader-backlitos/`。HTTP 验证额外锁定新资产语义，防止旧进程页面冒充新包；模拟失败后恢复成功才截图。

现场进一步发现一行多字段的任务头导致状态漏读，已补同行元数据与字段表解析，并对代码块/散文伪匹配作反证；S232 正确读为“施工中”。全套回归更新为 **125 项全部通过、无跳过**。独立复审另发现闲置退出后的页面重连入口缺失，已修为刷新失败显示连接按钮、恢复成功隐藏；源码 Chrome 七项复验通过（`output/reader-final-source/`）。

源文档归属不擅自改写，例如 P10-A 卡目前正本 parent_focus 指向 p9-next，页面如实沿用该归属，不推断它就是 P9 工程任务。没有发生业务任务自动采纳、归档或状态改写。

## 最终安装复验

- 最终完整包源：`f671b531f2c1fbd124e2fcbd85b2afac535f87bf`（已推送 origin/main）。包在 `output/release-v090/donghe-construction-team-v0.9.0-darwin-arm64.tar.gz`，SHA256 `beb20e2ccefd8c6e06a642d88b12a1172789b523eff976479a6a63e7b5b2d140`。
- 双端正式重装成功，manifest sourceCommit 与 reader 资产/解析器/SKILL 均匹配最终源码。此次安装前版本备份 `~/.donghe/backups/20260907-234517-58389709/`；需要回到升级前 v0.8.2 时使用首轮备份，而非此次 v0.9.0 中间版。
- 关闭本轮旧阅读进程后，由 macOS URL 协议实际唤起最终安装版本；当前项目 QA011 正确读取为“施工中，未验收”，S232 为“施工中”。根入口已重新生成。
- 最终安装包在回光项目 Chrome 七项验证再次通过，零页面错误；覆盖断线显示重连、恢复隐藏连接按钮、手机宽度、刷新、索引、原文。证据 `output/reader-backlitos-final/result.json` 与桌面/窄屏截图。
- 目标项目只新增根 HTML 派生入口，没有本轮写入业务代码、既有任务正本或第二套治理目录。入口不自动提交，避免将内部全文快照公开。
