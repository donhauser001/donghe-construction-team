# 任务卡 · YYYY-MM-DD-任务名

- 派给：工人#N · 模型：<slug>（为何这档够用：一句话）
- 目标：一句话说清做成什么样

## 写白名单（只许写这些）

- `path/to/file1.ts`
- `path/to/dir/**`

## 不变量（必须保持）

- 例：xxx API 入参 / 出参不变；xxx 表 schema 不动；xxx 权限校验不绕过

## 验收点与证据命令

- [ ] xxx（真实运行时验证，不是只看 build 过）
- [ ] xxx

```bash
python scripts/collect_evidence.py --cmd "typecheck::npm run typecheck" --cmd "test::npm test" --out docs/任务卡/_evidence/YYYY-MM-DD-任务名.md
```

## 工人回报区

- 已做：
- 未做 + 原因：
- 盲区（"我没读过 X / 未真实运行验证 Y"）：
- 证据文件：
